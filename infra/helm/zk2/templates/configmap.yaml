apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "zk2.fullname" . }}-config
  labels: {{- include "zk2.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
