# Smallest thing that runs: one of each, no autoscaling, no policies.
config:
  APP_ENV: dev
  LOG_LEVEL: DEBUG
  APP_BASE_URL: http://localhost:3000
  API_BASE_URL: http://localhost:8000
  STORAGE_KIND: local
image:
  tag: dev
ingress:
  enabled: false
